# React Native's Metro bundler cannot follow pnpm's nested symlinks for
# transitive peer dependencies. Hoisting public packages to the top-level
# node_modules makes Metro happy without losing pnpm's isolation entirely.
# See https://docs.expo.dev/guides/monorepos/.
node-linker=hoisted
